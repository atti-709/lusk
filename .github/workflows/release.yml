name: Release

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install dependencies
        run: npm install

      - name: Rebuild app-builder-bin for current arch
        run: npm rebuild app-builder-bin

      - name: Bump version
        run: npm version patch -w electron

      - name: Build all workspaces including Electron
        run: npm run build:electron

      - name: Bundle and publish
        working-directory: electron
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run bundle && npx electron-builder --publish always

      - name: Push version and tag
        run: git push --follow-tags
